#!/bin/bash

# omarchy:summary=Returns true when running on a 32-bit ARM (ARMv7) machine.

# OMARCHY_ARCH is what the image builder sets when it runs setup inside a
# qemu-user chroot, where uname reports whatever the emulator decided to say.
[[ ${OMARCHY_ARCH:-$(uname -m)} == armv7* ]]
